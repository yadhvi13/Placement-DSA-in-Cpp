class Solution {
public:
    int main(int nums) {

       int arr[];
       cin >> arr[];
       int nums = sizeof(arr)/ sizeof(arr[0]);

       int largest = arr[0];
       for(int i=0; i<nums; i++){
        if(arr[i]>largest){
            largest = arr[i];
        }
       }
    }
};